import React from 'react';

/**
 * Global notification system - simplified
 * Video generation is now handled by VideoToast in _layout.tsx
 */
export function GlobalNotifications() {
  // VideoToast is now rendered in _layout.tsx and handles all video generation state
  return null;
}